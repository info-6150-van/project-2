# Migration Instructions

This should not be needed for any testing users, and is more an instruction for myself to setup Supabase.

Run the numbered SQL files in the SQL editor tab on Supabase before running the app. This is needed if i somehow forgot the supabase account and need to set it up again.